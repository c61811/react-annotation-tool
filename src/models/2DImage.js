export class ImageAnnotation {
  constructor({id, name, color, x, y, width, height }) {
		this.id = id;
    this.name = name;
    this.color = color;
		this.x = x;
    this.y = y;
		this.width = width;
		this.height = height;
  }
}
